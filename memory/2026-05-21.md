1|## 2026-05-21
2|
3|- Nest planning source of truth moved to Linear in workspace "AI Kanban Pilot", team key `AI`. ROADMAP remains strategy/history; WORKLIST is frozen. Active tickets should be pulled from Linear states `Spec\'d` or `Working`, respecting `ai-ready`, `human-only`, `needs-spec`, `blocked`, and WIP limits.
4|- AI-45 completed and moved to Review at commit `919fc0c`: added `docs/ADR-002-skill-delivery.md`, a local Claude marketplace wrapper plugin `nest-skills`, `scripts/apply-skills.py`, and delivery of Nest skills via symlinked Claude-style `SKILL.md` directories. Claude Code 2.1.146 was verified to load symlinked Nest skills as `nest-skills:<skill-name>`.
5|- Decision for Nest skills: canonical source is `/opt/nest/skills/<name>/SKILL.md`; Claude-style frontmatter (`name`, `description`, markdown body) is canonical; symlinked wrapper plugin is preferred over generated copies unless a runtime cannot follow symlinks.
6|- AI-10 / C5 `server-overview` completed and moved to Review at commit `b596706`: skill now sources `/home/claude/.openclaw/.env` before calling the hub without printing the token. Claude Code smoke test reported server online, CPU/RAM fine, disk 71%, containers running.
7|- AI-13 / C8 `token-report` completed and moved to Review at commit `b334c69`: added `/opt/nest/skills/token-report/SKILL.md`, symlinked into `nest-skills`, and verified via Claude Code against `/api/observability/tokens`.
8|- Notable follow-up from token-report smoke test: Nest hub request waste was surfaced at 26.3%, worth a future Linear ticket or investigation.
9|- Current stop condition: Review WIP limit is exceeded/at capacity after AI-45, AI-10, and AI-13 are in Review. Do not auto-proceed to C6/C7 until the human clears/moves Review items or explicitly permits continuing despite the limit.
10|- Review lane at capacity (AI-45, AI-10, AI-13); C6/C7 progression paused until Review items are cleared or explicitly allowed.